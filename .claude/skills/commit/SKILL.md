---
name: commit
description: ガイドラインに従ってステージングされている変更をコミット
---

# コミットの作成

## 概要

`README.md`の`コミットメッセージ規約`に従ってステージングされている変更をコミットする。

---

## 実行手順

1. `git diff --cached` でステージングされている変更を確認
2. `README.md` のコミットメッセージ規約を読み込む
3. 規約に従ったコミットメッセージを作成してコミットする
