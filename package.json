{
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "lefthook": "^2.0.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=24"
  },
  "name": "family-photo",
  "packageManager": "pnpm@10.17.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "ci": "biome ci && turbo run typecheck",
    "dev": "turbo run dev",
    "format": "biome check --fix --unsafe . && turbo run format",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  }
}
